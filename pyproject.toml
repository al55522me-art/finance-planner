[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "finance-planner"
version = "0.1.0"
description = "Personal finance planner with OOP"
readme = "README.md"
requires-python = ">=3.10"